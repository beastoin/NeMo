apiVersion: v1
kind: Service
metadata:
  name: nemotron-stream
  namespace: dev-omi-backend
  labels:
    app: nemotron-stream
spec:
  type: ClusterIP
  selector:
    app: nemotron-stream
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
