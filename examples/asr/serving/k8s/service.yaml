apiVersion: v1
kind: Service
metadata:
  name: parakeet-batch
  namespace: dev-omi-backend
  labels:
    app: parakeet-batch
spec:
  type: ClusterIP
  selector:
    app: parakeet-batch
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
